import {PagedList} from 'react-hero';
import * as React from 'react';

export class <%= modelName%>ListPage extends React.Component<{}, void> {
    static resourceName: string = `<%= resourceName%>`;

    render(): JSX.Element {
        return (
            <div className="<%= resourceName%>-list-page">
                <PagedList resource={this.constructor[`resourceName`]} max={20}>
                    {/*<DropDownFilter
                        label="mention label here"
                        paramName="mention parameter to apply filter on"
                        possibleValues={[mention possible values here]}
                    />
                    <RangeFilter
                        label="mention label here"
                        paramName="mention parameter to apply filter on"
                    />
                    <DateRangeFilter
                        label="mention label here"
                        paramName="mention parameter to apply filter on"
                    />
                    <QueryFilter
                        label="mention label here"
                        paramName="mention parameter to apply filter on"
                        placeholder={[mention parameters which are queryable]}
                    />*/}
                </PagedList>
            </div>
        );
    }
}
