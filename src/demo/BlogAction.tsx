import * as React from 'react';

export class BlogAction extends React.Component<void, void> {
    render(): JSX.Element {
        return (
            <button>
                testAction
            </button>
        );
    }
}